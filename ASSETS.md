# Графика: что докинуть и куда это встанет

В коде уже есть слоты под каждую картинку — объект `ASSETS` в начале скрипта `index.html`.
Пока слот пустой, работает CSS-заглушка. Чтобы подключить файл: положить его в `assets/`
и вписать путь в соответствующее поле `ASSETS`. Больше ничего править не нужно —
`applyAssets()` сам включит нужные правила стиля.

```js
const ASSETS = {
  sky: "assets/stadium_night.webp",     // вот так
  ...
};
```

## Что вообще стоит рисовать

Три очереди по отдаче на вложенный труд. Верхняя очередь меняет картинку сильнее всего.

| Очередь | Ассет | Почему именно он |
|---|---|---|
| **1** | Фон стадиона | Сейчас это CSS-градиенты. Один хороший кадр меняет всю игру целиком — он под каждым экраном |
| **1** | Вратарь (6 кадров) | Самое слабое место: сейчас синяя капля из двух градиентов. И это пиковый момент матча |
| **1** | Мяч | Виден постоянно, ездит по лестнице зон. Сейчас `conic-gradient`, вблизи выглядит грубо |
| **2** | Ворота с сеткой | CSS-сетка читается, но плоская. Вместе с вратарём делает пенальти сценой, а не схемой |
| **2** | Комментатор (5 эмоций) | Единственное, что добавит игре характер. Персонаж реагирует на ваши ответы |
| **2** | Иконки категорий (6) | Сейчас эмодзи, и они разностильные: флаг России рядом с 🟨 выглядит случайно |
| **3** | Иконки инструментов (7) | Эмодзи ⚑🧥📣📺⏱⚡🅿 в разных шрифтах рисуются по-разному |
| **3** | Карточки ЖК/КК | CSS-градиент уже неплох, но настоящая карта с блеском и фактурой добьёт момент |
| **3** | Логотип, кубок, газон | Приятно, но игра и без них смотрится собранной |

**Чего рисовать НЕ надо.** Разметка поля, полосы газона, лестница зон, кольцо таймера, конфетти,
вспышки и табло — всё это CSS, оно масштабируется без потерь, весит ноль и уже выглядит как надо.
Заменять картинками — только хуже.

## Общие требования

- **Формат.** WebP для всего, где нужен вес (фоны, персонажи). PNG — если нужна безупречная
  альфа и файл небольшой. Для иконок можно SVG.
- **Прозрачность.** Всё, кроме фонов и газона, — с альфа-каналом.
- **Размеры ниже даны с запасом ×2** под retina. Больше не нужно: игра казуальная, вес важнее.
- **Палитра игры.** Лайм `#c8ff2e`, золото `#ffcf3d`, газон `#0d6b31`–`#0f7a38`, ночь `#050a10`.
  Жёлтый `#ffd60a` и красный `#ff3b30` в игре означают **только карточки** — не используйте их
  как акцент в другой графике, иначе игрок будет читать это как предупреждение.
- **Единый стиль.** Один художник или один промпт на весь набор. Разнобой заметнее, чем
  средний уровень исполнения.
- **Стиль, который сюда просится** — тот же, что в «Бедолаге»: сочный полуреалистичный 3D-рендер
  с тёплым светом и мягкими бликами. Он уже знаком по второй игре и хорошо продаётся на площадке.

## Спецификация по слотам

### Фон стадиона — `ASSETS.sky`, `ASSETS.skyPortrait`

| | |
|---|---|
| Файлы | `stadium_night.webp`, `stadium_night_portrait.webp` |
| Размер | 1920×1080 и 1080×1920 |
| Альфа | не нужна |

Ночной стадион с трибунами под прожекторами, вид с уровня поля или чуть выше. **Критично:**
центр кадра должен быть тёмным и спокойным — поверх него лежат панели с текстом. Свет и детали
уводите к верхним углам, куда смотрят прожекторы. Забитый деталями центр убьёт читаемость вопроса.

Вертикальный кадр — не кроп, а отдельная компоновка: на телефоне видно верхнюю треть экрана,
и там должны быть трибуны, а не пустое небо.

### Вратарь — `ASSETS.keeper.*` (6 файлов)

| | |
|---|---|
| Файлы | `keeper_idle`, `keeper_lean_l`, `keeper_lean_r`, `keeper_dive_l`, `keeper_dive_r`, `keeper_dive_c` |
| Размер | **1200×550 каждый, одинаковый холст** |
| Альфа | обязательна |

Главное правило: **все шесть кадров на одном холсте размером ворот, поза уже внутри кадра.**
Код не крутит и не сдвигает картинку — он просто меняет кадр. Поэтому в `dive_l` вратарь уже
нарисован в левом углу в полёте, и его тело может выходить за пределы створа.

Кадры по смыслу:
- `idle` — стойка в центре, руки раскрыты, вес на носках;
- `lean_l` / `lean_r` — тот же вратарь, корпус клонится в сторону. Это **подсказка игроку** перед
  ударом, наклон должен читаться однозначно и издалека;
- `dive_l` / `dive_r` — полноценный полёт в угол, тело почти горизонтально;
- `dive_c` — остался в центре, вытянулся вверх или сложился внизу.

Форма — яркая, но **не зелёная и не лаймовая**: он на фоне газона и рядом с лаймовым интерфейсом.
Оранжевый, бирюзовый, фуксия читаются идеально.

### Мяч — `ASSETS.ball`

`ball.webp`, 128×128, альфа. Один файл на всё: и на лестницу зон (там он 18 px), и на пенальти.
Значит, силуэт должен читаться в 18 пикселей — крупные контрастные пятна, никакой мелкой графики
и рекламных надписей. Свет сверху, лёгкая тень снизу.

### Ворота — `ASSETS.goal`

`goal.webp`, 1200×550, альфа. Тот же холст, что у вратаря — они лежат друг на друге.
Створ с сеткой, вид строго спереди, каркас белый. Сетка полупрозрачная: сквозь неё видно фон.
Нижнюю кромку не рисуйте — под ворота код подставляет полосу газона.

### Комментатор — `ASSETS.host.*` (5 файлов)

| | |
|---|---|
| Файлы | `host_idle`, `host_ok`, `host_bad`, `host_card`, `host_win` |
| Размер | 400×400 каждый |
| Альфа | не нужна (кадр обрезается в круг) |

Портрет по грудь, лицо крупно и по центру — на экране это круг ~40 px в строке трансляции.
Состояния: спокойный, довольный, огорчённый, строгий (на карточку), ликующий (на гол).
Эмоция должна читаться по одному лицу, без жестов — рук почти не видно.

Это самый недооценённый ассет: он превращает текстовую строку внизу в живого человека,
который радуется и расстраивается вместе с игроком.

### Иконки категорий — `ASSETS.cat.*` (6 файлов)

192×192, альфа. Ключи и смысл:

| Ключ | Категория | Что на иконке |
|---|---|---|
| `world` | Сборные и мундиали | глобус с мячом, кубок мира |
| `euro` | Еврокубки | ушастый кубок Лиги чемпионов |
| `leagues` | Европейские лиги | клубный щит, майка в полоску |
| `players` | Игроки и рекорды | футболка с номером, бутса |
| `rules` | Правила и судейство | свисток, карточки в руке судьи |
| `tactics` | Тактика и термины | тактическая доска со стрелками |
| `russia` | Российский футбол | узнаваемый мотив без политики |

Все семь в одной сетке, одной толщины линии, с одинаковым визуальным весом.

### Иконки фланга «ВАША ПОЗИЦИЯ» — `ASSETS.role.*` (4 файла)

192×192, альфа, та же сетка, что у категорий. Этот фланг игра предлагает по выбранному
амплуа, и он подписан золотом — иконки должны читаться как «своя», отдельная линейка:

| Ключ | Фланг | Что на иконке |
|---|---|---|
| `gk` | Вратари | перчатки, вратарский свитер |
| `def` | Защитники | щит, подкат |
| `mid` | Полузащитники | компас, схема передач |
| `fwd` | Нападающие | бутса в ударе, мяч в сетке |

Их видно рядом с категорийными иконками на одном экране, поэтому стиль общий,
а отличие — в цвете: тёплое золото против нейтрального лайма у категорий.

### Иконки инструментов — `ASSETS.tool.*` (7 файлов)

96×96, альфа. На экране ~20 px, поэтому силуэт простой и монохромный:

`offside` (флажок лайнсмена) · `coach` (тренер у кромки или планшет) · `stands` (сектор трибун,
рупор) · `var` (монитор или квадрат из пальцев судьи) · `time` (секундомер, табло замен) ·
`attack` (стрела вперёд) · `cash` (щит, замок, парковка мяча в углу)

### Карточки — `ASSETS.card.y`, `ASSETS.card.r`

500×735, альфа. Карта в руке или сама по себе, под небольшим углом, с бликом и лёгкой
фактурой пластика. Показывается на весь экран, поэтому детали видно.

### Логотип — `ASSETS.logo`

1440×440, альфа. Полный локап «15 ШАГОВ К МИЛЛИОНУ · ФУТБОЛ». Заменяет текстовую надпись
в меню целиком, так что типографика внутри — на вас. Работать должен на тёмном фоне.

### Кубок — `ASSETS.trophy`

300×300, альфа. Появляется на экране победы над суммой. Золото, блики, немного пафоса.

### Газон — `ASSETS.turf`

640×1440, без альфы. Текстура подстилается под лестницу зон вместо CSS-полос. Скошенная трава
полосами, лёгкий шум, без разметки — разметку код рисует сверху сам.

## Ассеты для Консоли Яндекс.Игр

Отдельная история: иконка игры и обложка для каталога нужны для публикации независимо от того,
что происходит внутри игры. Актуальные размеры и требования смотрите прямо в Консоли
разработчика — они менялись, и лучше брать оттуда, чем из документации по памяти.

## Проверка

Разложили файлы, вписали пути — откройте игру и проверьте в консоли браузера:

```js
document.documentElement.className
// покажет, какие слоты подхватились: "has-sky has-ball has-keeper ..."
```

Если ожидаемого `has-*` нет — либо путь с опечаткой, либо файл не там, где написано.
